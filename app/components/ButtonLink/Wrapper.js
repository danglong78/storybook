import styled from 'styled-components';
import { GAP_ELEMENT } from 'constants/styles';

const Wrapper = styled.div`
  padding: ${GAP_ELEMENT}px 0px;
  margin: 0 auto;
  user-select: none;
  cursor: pointer;
  outline: 0;
`;

export default Wrapper;
