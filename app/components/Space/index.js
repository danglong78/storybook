import styled from 'styled-components';
import { GAP_ELEMENT } from 'constants/styles';

const Space = styled.div`
  min-height: ${GAP_ELEMENT * 2}px;
  padding: 0;
  margin: 0;
`;

export default Space;
