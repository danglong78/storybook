import styled from 'styled-components';
import { GAP_ELEMENT } from 'constants/styles';

const H1 = styled.h1`
  font-style: normal;
  font-weight: bold;
  font-size: 40px;
  line-height: 42px;
  margin: ${GAP_ELEMENT * 2} auto ${GAP_ELEMENT * 3} auto;
`;

export default H1;
