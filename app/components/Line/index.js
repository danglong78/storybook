import styled from 'styled-components';
import { GAP_ELEMENT } from 'constants/styles';

const Line = styled.hr`
  border: 0.5px solid #2C3677;
  margin: ${GAP_ELEMENT}px auto;
`;

export default Line;
