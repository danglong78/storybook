import styled from 'styled-components';
import { GAP_ELEMENT } from 'constants/styles';

const P = styled.p`
  font-style: normal;
  font-weight: normal;
  font-size: 16px;
  line-height: 17px;
  margin: ${GAP_ELEMENT} auto;
`;

export default P;