import styled from 'styled-components';

const AvatarWrapper = styled.div`
  font-style: normal;
  font-weight: 500;
  font-size: 14px;
  line-height: 15px;

  color: #9F9ACB;
`;

export default AvatarWrapper;
