import styled from 'styled-components';

const AvatarWrapper = styled.div`
  margin-bottom: 4;
  font-style: normal;
  font-weight: 600;
  font-size: 14px;
  line-height: 15px;

  color: #1C1C28;
`;

export default AvatarWrapper;
